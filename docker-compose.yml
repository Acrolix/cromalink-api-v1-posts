services:
  api-posts:
    image: cromalink-api-v1-posts:latest
    build:
      target: api-posts
      context: .
    env_file:
      - .env
    ports:
      - "80:80"
    # depends_on:
    #   - db

#   db:
#     platform: "linux/amd64"
#     image: mysql:5.7
#     env_file:
#       - .env
#     environment:
#       MYSQL_ROOT_PASSWORD: root
#       MYSQL_DATABASE: ${DB_DATABASE}
#       MYSQL_USER: ${DB_USERNAME}
#       MYSQL_PASSWORD: ${DB_PASSWORD}
#     ports:
#       - "3306:3306"
#     volumes:
#       - dbdata:/var/lib/mysql

# volumes:
#   dbdata:
